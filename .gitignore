# Cached Groth16 keys, regenerated by zkp.LoadOrSetup.
artifacts/
# State written by cmd/agent and cmd/service.
agent-state/
# Binaries left by `go build ./cmd/<name>`.
/agent
/bench
/demo
/service
/web
# Installed by `npm install` in docs/slides.
docs/slides/node_modules/
# macOS and Office leftovers.
.DS_Store
~$*
# Claude Code's local workspace settings.
.claude/
